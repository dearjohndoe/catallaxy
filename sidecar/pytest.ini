[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    network: test hits external network (skip in offline runs with `pytest -m "not network"`)
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
